import type{MetadataRoute}from"next";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const b=process.env.NEXT_PUBLIC_SITE_URL||"https://www.clinicadentalalmagro.es";return["","/servicios","/proyectos","/sobre-nosotros","/contacto","/aviso-legal","/politica-de-privacidad","/politica-de-cookies"].map((p,i)=>({url:`${b}${p}`,lastModified:new Date(),changeFrequency:i?"monthly":"weekly",priority:i?.7:1}))}
