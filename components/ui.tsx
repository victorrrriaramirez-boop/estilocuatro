import Image from"next/image";import Link from"next/link";import{ArrowRight}from"lucide-react";import{cases,services}from"@/lib/data";export function PageHero({eyebrow,title,text}:{eyebrow:string,title:string,text:string}){return <section className="page-hero"><div className="container"><span className="chip">{eyebrow}</span><h1 className="title">{title}</h1><p className="lead">{text}</p></div></section>}export function ServicesGrid({limit}:{limit?:number}){return <div className="grid-3">{services.slice(0,limit).map(s=><article className="card service" id={s.slug} key={s.slug}><div className="icon"><s.icon/></div><h3>{s.title}</h3><p>{limit?s.short:s.detail}</p><Link className="more" href={`/contacto?tratamiento=${s.slug}`}>Consultar <ArrowRight size={13} style={{display:'inline'}}/></Link></article>)}</div>}export function CasesGrid(){return <div className="grid-3">{cases.map(c=><article className="card case" key={c.title}><Image src={c.image} alt={c.alt} width={512} height={279}/><div className="case-body"><span className="tag">{c.tag}</span><h3>{c.title}</h3><p>{c.text}</p><p className="result">Resultado documentado y seguimiento clínico</p></div></article>)}</div>}export function CTA(){return <section className="section peach"><div className="container"><span className="chip">Primera visita</span><h2 className="title-md">¿Hablamos de tu sonrisa?</h2><p className="lead">Cuéntanos qué necesitas y te explicaremos cómo podemos ayudarte.</p><div className="actions"><Link className="btn" href="/contacto">Solicitar cita</Link><a className="btn ghost" href="tel:+34914458920">Llamar a la clínica</a></div></div></section>}
